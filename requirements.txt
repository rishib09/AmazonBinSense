# BinSense AI — Python dependencies
# Target runtime: Python 3.11  (matches Google Colab; do NOT use 3.14 locally)
#
# Install locally:
#   python3.11 -m pip install -r requirements.txt
#
# Colab: the setup/install_colab.sh cell handles this automatically.
#
# FAISS note:  use faiss-cpu here (CPU-only).
#              On Colab with GPU runtime, the install cell swaps in faiss-gpu.
#
# Torch note:  CPU wheels listed here for portability.
#              On Colab the runtime already has CUDA torch; skip torch lines there.

# ── Core PyTorch (CPU wheels — override on Colab/GPU machines) ────────────────
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# ── Computer vision & detection ───────────────────────────────────────────────
ultralytics>=8.2.0          # YOLOv8 / YOLOv11
opencv-python-headless>=4.9.0
Pillow>=10.3.0
timm>=1.0.3                 # pretrained backbones (ResNet50, MobileNetV2, etc.)

# ── Segmentation (SAM) ────────────────────────────────────────────────────────
# Install separately (no PyPI wheel; handled in install_colab.sh):
#   pip install git+https://github.com/facebookresearch/segment-anything.git

# ── Retrieval / metric learning ───────────────────────────────────────────────
faiss-cpu>=1.8.0            # swap for faiss-gpu on GPU Colab runtime
scipy>=1.13.0               # Hungarian algorithm: scipy.optimize.linear_sum_assignment
scikit-learn>=1.5.0

# ── Data / EDA ────────────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.9.0
seaborn>=0.13.0
tqdm>=4.66.0

# ── AWS / S3 access (anonymous bucket aft-vbi-pds) ───────────────────────────
boto3>=1.34.0
botocore>=1.34.0
requests>=2.32.0

# ── Notebook / interactive ────────────────────────────────────────────────────
jupyter>=1.0.0
jupyterlab>=4.2.0
ipywidgets>=8.1.0
ipython>=8.25.0

# ── MLOps ─────────────────────────────────────────────────────────────────────
mlflow>=2.13.0
prometheus-client>=0.20.0

# ── App UI (choose one; both listed for dev flexibility) ─────────────────────
streamlit>=1.35.0
gradio>=4.36.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0.1
