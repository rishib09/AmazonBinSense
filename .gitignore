# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Virtual environments — NEVER commit; lives at C:\Users\rishi\envs\binsense
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data — large files stay on Google Drive, not in git
data/images/
data/metadata/
data/labels/
data/labels_staging/     # auto-label output awaiting human review (M3)
data/yolo/               # generated YOLO dataset manifest (abs paths; not portable)
# data/splits/ is intentionally tracked — split CSVs are small and define
# the seed/extend/eval partition (reproducibility artifact).
mlruns/
reports/                 # QA artifacts (label overlays, etc.)

# Models — saved to Drive/registry, not git
models/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
