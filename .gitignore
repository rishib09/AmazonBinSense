# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Virtual environments — NEVER commit; lives at C:\Users\rishi\envs\binsense
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data — large files stay on Google Drive, not in git
data/images/
data/metadata/
data/labels/
# auto-label output awaiting human review (M3)
data/labels_staging/
# M3b auto-label pipeline: raw per-method boxes + Track 1/2 label sets
data/labels_auto/
data/labels_track1/
data/labels_track2/
# generated YOLO dataset manifests (abs paths; not portable)
data/yolo/
data/yolo_track1/
data/yolo_track2/
# data/splits/ is intentionally tracked — split CSVs are small and define
# the seed/extend/eval partition (reproducibility artifact).
mlruns/
# QA artifacts (label overlays, etc.)
reports/

# Models — saved to Drive/registry, not git
models/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
